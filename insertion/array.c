#include<stdio.h>
#define max 5

int main()
{
    int a[max]={1,2,3};
    int n=size of(a)/size of(a[0]);
    printf("elements in array bfr insertion are:")}
    {nrntraverse(a,n);
    //insert at beginning
    n=insertat beg(a,n);


return n;
    }





